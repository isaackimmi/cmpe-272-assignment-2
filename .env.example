# Copy to .env and supply real values locally. Never commit .env.
GITHUB_TOKEN=your_fine_grained_github_pat
GITHUB_OWNER=isaackimmi
GITHUB_REPO=cmpe-272-assignment-2
WEBHOOK_SECRET=generate_a_random_secret
HOST=127.0.0.1
PORT=3000

# Gateway bearer authentication; distinct from the GitHub token.
API_AUTH_TOKEN=generate_a_different_random_secret

# Optional overrides; the defaults are shown here for clarity.
EVENTS_DATABASE_PATH=./data/events.sqlite
